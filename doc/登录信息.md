# 数据资产管理系统 - 登录信息速查�?

> [!IMPORTANT]
> **文档维护提示**：当修改 `docker-compose.yml` 中的密码、端口、数据库名称等配置时，请务必同步更新本文档！

## 🌐 服务访问地址

### 系统IP地址
- **主IP**: `192.168.2.170`
- **备用IP**: `192.168.1.77`

---

## 📋 服务清单

| 服务名称 | 端口 | 访问地址 | 用�?|
|---------|------|---------|------|
| **资产检索前�?* | 8082 | http://192.168.2.170:8082 | 资产检索主界面 |
| **资产检索后�?* | 8003 | http://192.168.2.170:8003/docs | API接口文档 |
| PostgreSQL | 5432 | 数据库端�?| 资产元数据存�?|
| pgAdmin 4 | 18080 | http://192.168.2.170:18080 | 数据库管理界�?|
| FileBrowser | 8081 | http://192.168.2.170:8081 | 文件管理界面 |

---

## 🔐 登录凭据

### 1️⃣ Web资产检索系统（无需登录�?

**前端访问地址**�?
```
http://192.168.2.170:8082
```

**说明**�?
- 直接访问即可使用，无需登录认证
- 支持资产搜索、详情查看、批量下�?
- 支持卡片视图和列表视图切�?

**API文档**�?
```
http://192.168.2.170:8003/docs
```

---

### 2️⃣ PostgreSQL 数据�?

**连接信息**�?
- **主机地址**: `192.168.2.170` �?`db`（Docker内部�?
- **端口**: `5432`
- **数据库名**: `asset_catalog`
- **用户�?*: `admin`
- **密码**: `Huaiye@2020**`

**连接字符串示�?*�?
```
postgresql://admin:Huaiye@2020**@192.168.2.170:5432/asset_catalog
```

---

### 3️⃣ pgAdmin 4 数据库管理界�?

**访问地址**�?
```
http://192.168.2.170:18080
```

**登录凭据**�?
- **邮箱**: `admin@admin.com`
- **密码**: `Huaiye@2020**`

**首次使用**�?
登录后需要添加PostgreSQL服务器连接：
1. 点击 **Servers** �?右键 �?**Register** �?**Server**
2. **General** 选项卡：Name填写 `数据资产管理数据库`
3. **Connection** 选项卡：
   - Host: `db`
   - Port: `5432`
   - Database: `asset_catalog`
   - Username: `admin`
   - Password: `Huaiye@2020**`
   - �?勾�?Save password

---

### 4️⃣ FileBrowser 文件管理界面

**访问地址**�?
```
http://192.168.2.170:8081
```

**登录凭据**�?
- **用户�?*: `admin`
- **密码**: `huaiye2020**`（如已修改）

**如果忘记密码，查看自动生成的密码**�?

在服务器上执行以下命令查看日志：
```bash
# 方法1：直接查找密�?
docker-compose logs filebrowser | grep password

# 方法2：查看详细信�?
docker-compose logs filebrowser | grep -A 2 "randomly generated password"
```

输出示例�?
```
asset_file_manager  | User 'admin' initialized with randomly generated password: npjOb4fU4hEMf3Jx
```

**重置FileBrowser密码**�?

如果日志中找不到密码，可以重置：
```bash
cd /opt/data_asset

# 停止服务
docker-compose stop filebrowser

# 删除配置数据�?
rm -rf ./filebrowser_data

# 重新启动（会生成新密码）
docker-compose up -d filebrowser

# 查看新密�?
docker-compose logs filebrowser | grep password
```

**说明**�?
- 首次部署后密码为随机生成，需通过日志查看
- 登录后请�?**设置** �?**用户管理** 中修改密�?
- 管理的文件路径：`/data/nas_data`

---

## 🔧 服务管理命令

### 查看服务状�?
```bash
cd /opt/data_asset
docker-compose ps
```

### 启动所有服�?
```bash
docker-compose up -d
```

### 停止所有服�?
```bash
docker-compose down
```

### 重启单个服务
```bash
docker-compose restart pgadmin
docker-compose restart filebrowser
docker-compose restart db
```

### 查看服务日志
```bash
# 查看所有日�?
docker-compose logs

# 查看特定服务日志
docker-compose logs pgadmin
docker-compose logs filebrowser
docker-compose logs db

# 实时查看日志
docker-compose logs -f pgadmin
```

---

## 🔒 安全建议

1. **修改默认密码**�?
   - 首次登录FileBrowser后立即修改密�?
   - 定期更换数据库密�?

2. **防火墙配�?*�?
   - 仅开放必要端口：18080�?081
   - 限制访问IP范围（如仅允许内网访问）

3. **备份重要数据**�?
   - PostgreSQL数据：`./pg_data/`
   - FileBrowser配置：`./filebrowser_data/`
   - pgAdmin配置：`./pgadmin_data/`

---

## 📞 故障排查

### FileBrowser无法登录
```bash
# 重置FileBrowser
docker-compose stop filebrowser
rm -rf ./filebrowser_data
docker-compose up -d filebrowser

# 查看日志获取新密�?
docker-compose logs filebrowser | grep password
```

### pgAdmin无法访问
```bash
# 检查服务状�?
docker-compose ps pgadmin

# 查看错误日志
docker-compose logs pgadmin --tail 50

# 重启服务
docker-compose restart pgadmin
```

### PostgreSQL连接失败
```bash
# 检查数据库状�?
docker-compose ps db

# 测试数据库连�?
docker-compose exec db psql -U admin -d asset_catalog

# 查看数据库日�?
docker-compose logs db
```

---

## 📝 数据存储位置

| 数据类型 | 宿主机路�?| 说明 |
|---------|-----------|------|
| NAS文件数据 | `/data/nas_data/` | 物理文件存储 |
| PostgreSQL数据 | `/opt/data_asset/pg_data/` | 数据库文�?|
| pgAdmin配置 | `/opt/data_asset/pgadmin_data/` | 管理界面配置 |
| FileBrowser配置 | `/opt/data_asset/filebrowser_data/` | 文件管理器配�?|

---

## 📅 文档更新日期

- **创建日期**: 2026-01-05
- **最后更�?*: 2026-01-05
- **系统版本**: Docker Compose v2

---

## ⚙️ 配置变更同步检查清�?

当修改以下配置时，请同步更新本文档：

- [ ] 修改�?`docker-compose.yml` 中的端口映射
- [ ] 修改了数据库用户名或密码
- [ ] 修改了数据库名称
- [ ] 修改了pgAdmin或FileBrowser的登录凭�?
- [ ] 更改了数据存储路�?
- [ ] 添加或删除了服务

